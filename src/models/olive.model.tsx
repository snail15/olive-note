class Olive {
  name: string;
  origin: string;
  id: number;

  constructor(name: string, origin: string, id: number) {
    this.name = name;
    this.origin = origin;
    this.id = id;
  }
}

export default Olive;
